import './StatsHugeBox.css';

const StatsHugeBox = ({ children }) => {

    return (
        <div className="stats-huge-box">
            {children}
        </div>
    );
};

export default StatsHugeBox;